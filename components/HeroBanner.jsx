import React from "react";


const HeroBanner = ({ heroBanner }) => {
    console.log(heroBanner.image.asset._ref)
    return (
      <div className="hero__img">
   
      </div>
    );
};

export default HeroBanner;
